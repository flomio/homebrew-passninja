# Homebrew tap for passninja

Install:

```sh
brew tap flomio/passninja
brew install passninja
```

The CLI source lives at https://github.com/flomio/passninja-cli. This tap is
bumped automatically by the CLI repo's release workflow on every tagged release.
